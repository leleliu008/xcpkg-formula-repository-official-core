summary: C XSLT library for GNOME
web-url: http://xmlsoft.org/XSLT
git-url: https://github.com/GNOME/libxslt
src-url: https://download.gnome.org/sources/libxslt/1.1/libxslt-1.1.43.tar.xz
src-sha: 5a3d6b383ca5afc235b171118e90f5ff6aa27e9fea3303065231a6d403f0183a
license: X11
dep-pkg: libxml2

dopatch: |
    gsed -i 's|$(XSLTPROC) --nonet $(srcdir)/xsltproc.xml|touch xsltproc.1|' doc/Makefile.am
    gsed -i 's|$(XSLTPROC) --nonet $(srcdir)/xsltproc.xml|touch xsltproc.1|' doc/Makefile.in

install: |
    configure \
        --without-debug \
        --without-mem-debug \
        --without-debugger \
        --without-crypto \
        --without-python \
        --with-profiler \
        --with-plugins
