summary: Play, record, convert, and stream audio and video
git-url: https://github.com/FFmpeg/FFmpeg
web-url: https://ffmpeg.org/
src-url: https://ffmpeg.org/releases/ffmpeg-6.1.1.tar.xz
src-sha: 8684f4b00f94b85461884c3719382f1261f0d9eb3d59640a1f4ac0873616f968
license: GPL-2.0-or-later
dep-pkg: libmp3lame libtheora libspeex libdav1d librav1e libopus libxvid libx264 libx265 libvpx libass libbluray librist libsoxr libzmq libvidstab frei0r gnutls aom opencore-amr rubberband snappy srt tesseract zimg alsa-lib xorg-libxv

bsystem: gmake
binbstd: 1
install: |
    run ./configure \
        --prefix=$PACKAGE_INSTALL_DIR \
        --ar=$AR \
        --as=$AS \
        --ld=$CC \
        --cc=$CC \
        --cxx=$CXX \
        --nm=$NM \
        --ranlib=$RANLIB \
        --strip=$STRIP \
        --enable-pic \
        --enable-static \
        --enable-shared \
        --enable-pthreads \
        --enable-version3 \
        --disable-asm \
        --disable-doc \
        --disable-debug \
        --enable-ffplay \
        --enable-gnutls \
        --enable-gpl \
        --enable-libaom \
        --enable-libbluray \
        --enable-libdav1d \
        --enable-libmp3lame \
        --enable-libopus \
        --enable-librav1e \
        --enable-librist \
        --enable-librubberband \
        --enable-libsnappy \
        --enable-libsrt \
        --enable-libtesseract \
        --enable-libtheora \
        --enable-libvidstab \
        --enable-libvmaf \
        --enable-libvorbis \
        --enable-libvpx \
        --enable-libwebp \
        --enable-libx264 \
        --enable-libx265 \
        --enable-libxml2 \
        --enable-libxvid \
        --enable-lzma \
        --enable-libfontconfig \
        --enable-libfreetype \
        --enable-frei0r \
        --enable-libass \
        --enable-libopencore-amrnb \
        --enable-libopencore-amrwb \
        --enable-libopenjpeg \
        --enable-libspeex \
        --enable-libsoxr \
        --enable-libzmq \
        --enable-libzimg \
        --disable-libjack \
        --disable-indev=jack
    gmakew clean
    gmakew install
    cp ffbuild/config.log "$PACKAGE_BCACHED_DIR/"
