summary: Fast, simple fuzzy text selector with an advanced scoring algorithm
git-url: https://github.com/jhawthorn/fzy
license: MIT
binbstd: yes
install: |
    gmakew clean &&
    gmakew CCFLAGS="'$LDFLAGS'" &&
    gmakew install PREFIX=$PACKAGE_INSTALL_DIR
